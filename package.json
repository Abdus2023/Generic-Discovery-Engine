{
  "name": "generic-discovery-engine",
  "version": "0.7.5",
  "description": "Browser-based discovery engine inspired by DVB blind scanning — trust & verification",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/generic-discovery-engine.user.js && node --check dist/generic-discovery-engine.v0.7.1.user.js",
    "verify": "npm run check && npm test",
    "lint": "node --check dist/generic-discovery-engine.user.js && echo \"lint: syntax OK (eslint optional)\"",
    "format:check": "prettier --check \"dist/*.js\" \"tests/*.js\" 2>&1 | head -n 20 || echo \"prettier not installed — skipping\""
  },
  "keywords": [
    "discovery",
    "dvb",
    "blind-scan",
    "userscript",
    "tampermonkey"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
