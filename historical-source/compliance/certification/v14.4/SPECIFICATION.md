# v14.4 — Deterministic Authority, Decision Validity & Causal Projection Protocol

## 1. Objective
Machine-normative definition: `OBJECTIVE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 2. Resolved contradictions
Machine-normative definition: `RESOLVED-CONTRADICTIONS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 3. Canonical state model
Machine-normative definition: `CANONICAL-STATE-MODEL.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 4. Certificate candidate model
Machine-normative definition: `CERTIFICATE-CANDIDATE-MODEL.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 5. Certificate lifecycle
Machine-normative definition: `CERTIFICATE-LIFECYCLE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 6. Certificate validity
Machine-normative definition: `CERTIFICATE-VALIDITY.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 7. Release identity
Machine-normative definition: `RELEASE-IDENTITY.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 8. Release gate
Machine-normative definition: `RELEASE-GATE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 9. Release eligibility
Machine-normative definition: `RELEASE-ELIGIBILITY.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 10. Release authority
Machine-normative definition: `RELEASE-AUTHORITY.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 11. Release decision
Machine-normative definition: `RELEASE-DECISION.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 12. Release execution
Machine-normative definition: `RELEASE-EXECUTION.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 13. Post-release verification
Machine-normative definition: `POST-RELEASE-VERIFICATION.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 14. Event/history model
Machine-normative definition: `EVENT/HISTORY-MODEL.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 15. Current projections
Machine-normative definition: `CURRENT-PROJECTIONS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 16. Temporal model
Machine-normative definition: `TEMPORAL-MODEL.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 17. Identity and binding
Machine-normative definition: `IDENTITY-AND-BINDING.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 18. Deterministic precedence
Machine-normative definition: `DETERMINISTIC-PRECEDENCE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 19. Validation procedure
Machine-normative definition: `VALIDATION-PROCEDURE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 20. Complete schemas
Machine-normative definition: `COMPLETE-SCHEMAS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 21. State-transition tables
Machine-normative definition: `STATE-TRANSITION-TABLES.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 22. Invariants
Machine-normative definition: `INVARIANTS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 23. Test vectors
Machine-normative definition: `TEST-VECTORS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 24. Traceability
Machine-normative definition: `TRACEABILITY.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 25. Failure modes
Machine-normative definition: `FAILURE-MODES.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 26. Revised artifact tree
Machine-normative definition: `REVISED-ARTIFACT-TREE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 27. Open questions
Machine-normative definition: `OPEN-QUESTIONS.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.

## 28. Final principle
Machine-normative definition: `FINAL-PRINCIPLE.yaml` where present; otherwise the corresponding registered schema or model artifact is authoritative.
