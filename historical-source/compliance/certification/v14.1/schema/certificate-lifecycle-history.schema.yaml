{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CertificateLifecycleHistoryV141",
  "type": "object",
  "required": [
    "history_id",
    "certificate_id",
    "transitions",
    "schema_version",
    "history_hash"
  ],
  "properties": {
    "history_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "certificate_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "transitions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "certificate-lifecycle-transition.schema.yaml"
      }
    },
    "schema_version": {
      "const": "14.1"
    },
    "history_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    }
  },
  "additionalProperties": false,
  "$id": "https://generic-discovery-engine.invalid/compliance/v14.1/certificate-lifecycle-history.schema.yaml"
}
