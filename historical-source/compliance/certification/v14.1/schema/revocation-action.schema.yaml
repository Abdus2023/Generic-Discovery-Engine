{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RevocationActionV141",
  "type": "object",
  "required": [
    "revocation_id",
    "certificate_id",
    "reason",
    "authority",
    "evidence_refs",
    "revoked_at",
    "schema_version",
    "revocation_hash"
  ],
  "properties": {
    "revocation_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "certificate_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "reason": {
      "type": "string",
      "minLength": 1
    },
    "authority": {
      "$ref": "certification-authority.schema.yaml"
    },
    "evidence_refs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "schema_version": {
      "const": "14.1"
    },
    "revocation_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    }
  },
  "additionalProperties": false,
  "$id": "https://generic-discovery-engine.invalid/compliance/v14.1/revocation-action.schema.yaml"
}
