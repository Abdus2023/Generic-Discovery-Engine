{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ReleaseExecutionV143",
  "type": "object",
  "required": [
    "execution_id",
    "release_id",
    "decision_id",
    "release_hash",
    "artifact_hash",
    "status",
    "attempt",
    "started_at",
    "completed_at",
    "result_hash",
    "schema_version",
    "execution_hash"
  ],
  "properties": {
    "execution_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "release_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "decision_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
    },
    "release_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "artifact_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "status": {
      "type": "string",
      "enum": [
        "NOT_STARTED",
        "STARTED",
        "SUCCEEDED",
        "FAILED",
        "CANCELLED",
        "ROLLED_BACK"
      ]
    },
    "attempt": {
      "type": "integer",
      "minimum": 0
    },
    "started_at": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "null"
        }
      ]
    },
    "completed_at": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "null"
        }
      ]
    },
    "result_hash": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        {
          "type": "null"
        }
      ]
    },
    "schema_version": {
      "const": "14.3"
    },
    "execution_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    }
  },
  "additionalProperties": false,
  "$id": "https://generic-discovery-engine.invalid/compliance/v14.3/release-execution.schema.yaml"
}
