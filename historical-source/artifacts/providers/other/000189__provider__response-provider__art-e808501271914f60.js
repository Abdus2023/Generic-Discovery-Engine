    class ResponseProvider {

        score(/* observation */) {
            return 0;
        }

        recognize(
            /* candidate, observation */
        ) {
            return null;
        }

        candidates(
            /* discovery */
        ) {
            return [];
        }
    }
