const context = {
    goal,
    domain,
    partition,
    step,
    execution,

    budget,
    cursor,

    proposeCandidate,
    scheduleProbe,
    emitEvidence,

    cancellation
};
