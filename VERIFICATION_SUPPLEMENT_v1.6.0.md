# Verification Supplement — v1.6.0 (Bounded Knowledge Kernel — B1→B3 retention coherence)

**Artifact:** `dist/generic-discovery-engine.user.js` v1.6.0 `6794 lines` `223914B` `sha256 7efbfd6bcb60b84b6a57ca57310262b4683cdd395b58ba1aa08ac64ff9d7cc84` + `dist/generic-discovery-engine.min.js` `74783B` `33.4%` `sha ba83e9f3f06fe2697fdb23ef7a7886239f9e640c5bd447097771a486feee3b06` `241 lines` + `dist/generic-discovery-engine.esm.js` `267B` `sha 79278308da66b46aa56eeb68f32248597c8b50cb603ce5a0450dace3bd354951`  
**Source:** `src/` 7 modules `config 220 + utils 356 + ledger 271 + models 377 + knowledge 1019 + providers 1285 + engine 2980 ≈ 6794` `package 1.6.0` `src/header.txt` `v1.6.0 — Bounded Knowledge Kernel (B1→B3)`  
**Tests:** `npm test` **160/160 42 suites** `node --check` PASS `verify:build` PASS (retention + body + GM gates) `coverage:check` PASS (honest 0/0/0) `lint` PASS `typecheck` informational `build:esm` `analyze-bundle` 18.9%

## § Bounded retention (B2+B3 vs B1 containers)
- **RetentionPolicy:** `CONFIG.retention` unified `{visited2k, candidateKeys2k, candidateHistory2k, observations800+5M/150 bodies, discoveries2k, resources2k×100, fingerprint500×100, patterns500, clusters500, networkEvents1k, graphEdges5k, diagnostics500}` + `runtimeBudget.maxBodyBytes 5M` mirrors `maxBodiesInMemory 150`. Additive to legacy `max*` (backwards compatible, `retention` overrides).
- **ResourceRecord:** `merge()` now `maxRelations 100` + `.slice(-100)` after `unique` for `types/mechanisms/parents/candidateIds/observationIds/discoveryIds/networkEventIds` (was unbounded `2000×∞`).
- **KnowledgeBase Kernel 1019 vs 744 (+275):** `_enforceVisitedBound` FIFO `visited-evicted`, `_enforceCandidateKeysBound` historical-first (preserves live `queued/claimed/planned/acquiring/observed/recognized` frontier, diagnostic `candidateKeys-evicted`/`candidateKeys-pressure`), `_enforceCandidateHistoryBound` evicts oldest `completed/skipped` FIFO + cleans `candidateKeys`/`claimed`/`resource.candidateIds` via `_removeCandidateReferences` (`candidate-history-evicted`), `addCandidate` cleans stale `candidateKeys→missing` (`candidateKeys-stale-cleaned`) and enforces both bounds after insert + `recordPattern→_enforcePatternBound` (`pattern-evicted`/`cluster-evicted`), `markCompleted/markSkipped` enforce `visited+history` after `visited.add`, `recordObservation` FIFO `observation-evicted` **with** `_removeObservationReferences` cleanup + `_enforceFingerprintBound` (500 hashes FIFO, 100 urls/hash `fingerprint-url-evicted`) + `_enforceBodyBudget` FIFO clears oldest `body=""` `bodyTruncated=true` while `>150 bodies||>5M bytes` (`body-evicted`, was count only), `addDiscovery` FIFO `_removeDiscoveryReferences` (`discovery-evicted`), `ensureResource` FIFO `_removeResourceFingerprint` (`resource-evicted`), `addEdge` FIFO shift `graphEdge-evicted` (was silent drop), `serialize()` `visited.slice(-2k)` (was `[...visited]` unbounded), `restore()` bounded restoration enforcing all caps after load (`observation-evicted-on-restore` etc., was direct insert → `5000 persisted` could bypass `2000 runtime`).
- **Referential coherence:** Helpers `_removeObservationReferences/_removeDiscoveryReferences/_removeCandidateReferences/_removeResourceFingerprint` loop `resources ≤2k` O(2k) to splice dangling IDs + stale `candidateKeys` auto-cleaned. Formal `candidateKeys[k]=id ⇒ candidates.has(id)` or explicit `stale-cleaned`; `Resource.discoveryIds→D` always resolves or historical.

## § Security (GM redirect)
- **GM:** `GM_xmlhttpRequest.onload` now checks `finalUrl = response.finalUrl||plan.target`; if `sameOriginOnly && finalUrl!==plan.target && !isAllowedUrl(canonicalizeUrl(finalUrl))` → `ledger gm-redirect-blocked` + `Observation{status:blocked,reason:redirect-cross-origin}` and return (mirrors `fetch redirect:'error'`). `recordNetworkEvent` now reads `retention.networkEvents.maxEntries ?? maxNetworkEvents`.

## § Build
- `src/header.txt` `1.5.0→1.6.0` patch notes vs 1.5 (retention+slice+kernel+GM + 8 verify gates), `package 1.6.0` `32 ADRs`, `scripts/verify-build.js` adds `CONFIG.retention` `maxBodyBytes` `maxRelationsPerResource` `body-evicted` `gm-redirect-blocked` `_enforceVisitedBound` `_removeObservationReferences` `observation-evicted-on-restore`. New build `6794 lines 223914B sha 7efbfd…` `min 74783 33.4% ba83e9…` `esm 267 792783` providers now `18.9%` (vs 20.6%) due to kernel +275 lines.

## § Tests & perf
- Tests still **160/160 42 suites** (no regressions); new stress **12/12** (see §Bounded retention): relation cap 100, body budget 5M/150 (50 bodies retained of 200×100k), visited 2k, discovery/observation dangling cleaned, fingerprint 500×100, pattern 500, restore 2k/800/5k bounded, GM blocked, stale cleaned, history 2k, edges 5k proves B3 semantically, not merely present. `coverage:check` still honest `0/0/0`.

## § Historical
- `VERIFICATION_SUPPLEMENT_v1.5.0.md` 160/160 6453 `508e3e…` remains valid parent, `DEEP_VERIFICATION_v1.4.md` 26 sections archived (second-pass audit now closed). `v1.6.0` delta `+341` lines `7efbfd…` + `verify:build` 8 new gates = B3 complete.

Refs: `package.json` `1.6.0` `dist/.build-meta.json` `7efbfd…`/`ba83e9…`/`792783…` `6794/241/267` `ADRs 32`.
